# Unreal Engine Project

This is an Unreal Engine 5.8 project. The UE Editor MCP server is configured.
When you need to interact with the Unreal Editor (e.g., modify assets, query actor info,
run editor commands), use the `unreal-engine` MCP tools.
